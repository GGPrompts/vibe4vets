# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Admin API key (server-side only - NOT exposed to browser)
# This key is used by the /api/admin proxy to authenticate with the backend.
# The backend validates this by comparing its SHA-256 hash to ADMIN_API_KEY_HASH.
ADMIN_API_KEY=
